# IP del portatile sulla LAN (vedi apps/api/.env.example). Se i frontend girano
# sullo stesso portatile del backend, il default (stesso hostname:3000) va bene.
VITE_API_URL=http://192.168.1.50:3000/api/v1
