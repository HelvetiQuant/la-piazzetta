{
  "name": "la-piazzetta",
  "version": "0.17.0",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "cd apps/api && npm run dev",
    "build": "npm run build --workspaces --if-present",
    "ci": "bash scripts/ci.sh all",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
